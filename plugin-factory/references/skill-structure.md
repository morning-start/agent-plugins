# Skill Structure — 共享骨架标题声明

本文件声明 plugin-factory 各技能共享的**骨架标题**（structure），供
`tools/verify/verify.mjs` 的 `repeated-guidance` 探针识别：声明在此处的 `##`
标题视为技能骨架结构，而非跨技能重复的指导内容（与 `SKELETON_HEADINGS`
同等处理，见 `tools/verify/verify.mjs`）。新增技能时应沿用这些骨架标题。

## Iron Law

## Red Flags — STOP and Rethink

## 自检清单 (Post-routing Self-Check)
