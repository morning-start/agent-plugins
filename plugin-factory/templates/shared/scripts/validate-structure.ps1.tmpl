# validate-structure.ps1 — structural checks for generated plugin projects.
# Thin wrapper around scripts/verify.mjs (single cross-platform engine).
$root = Split-Path -Parent $PSScriptRoot
& node "$root/scripts/verify.mjs" structure --root "$root"
exit $LASTEXITCODE
