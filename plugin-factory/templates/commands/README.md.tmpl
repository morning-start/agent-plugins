---
description: Placeholder index for this plugin's slash commands — replace with real command files.
---

# commands/

Slash commands for this plugin. Generated by plugin-factory from the component
manifest — one `*.md` per command with a frontmatter `description`.

Placeholder: replace with real commands (e.g. `{{PLUGIN_PREFIX}}-<action>.md`).
