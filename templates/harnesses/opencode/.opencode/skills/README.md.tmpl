# .opencode/skills/

opencode discovers skills in this directory (also `.claude/skills/` and
`.agents/skills/`). The scaffold copies each skill from the canonical root
`skills/` tree here so opencode can load them without a manual copy step.

Re-run the scaffold (or `pf-build`) to refresh this directory when skills change.
