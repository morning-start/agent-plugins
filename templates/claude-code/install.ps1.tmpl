# Install {{PLUGIN_NAME}} for Claude Code.
$src = Split-Path -Parent $MyInvocation.MyCommand.Path

Write-Host "Option A - run Claude Code with this plugin loaded:"
Write-Host "  claude --plugin-dir $src"
Write-Host ""
Write-Host "Option B - distribute via a marketplace (see references: plugin-marketplaces)."
