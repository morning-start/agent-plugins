# plugin-factory session-start bootstrap (PowerShell variant).
# Reminds the agent to check pf-* skills before starting any task.

Write-Host "[plugin-factory] Before any task, check whether a pf-* skill applies (see skills/)."
