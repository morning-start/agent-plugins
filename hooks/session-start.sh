#!/usr/bin/env bash
# plugin-factory session-start bootstrap (bash variant).
# Reminds the agent to check pf-* skills before starting any task.

echo "[plugin-factory] Before any task, check whether a pf-* skill applies (see skills/)."
