#!/usr/bin/env bash
# Git pre-push hook — install via: git config core.hooksPath .githooks
# Delegates to hooks/pre-push.sh for the actual checks
set -euo pipefail

HOOK_DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec bash "${HOOK_DIR}/hooks/pre-push.sh"